node_modules/
dist/
*.log
out/
demo/node_modules/
demo/clips/*
# **샘플 하나는 넣는다.** 받는 사람이 바로 눌러볼 것이 있어야 한다.
# 직접 찍은 보행 영상의 30~50초 구간이다 (360x640, 20초, 2.2MB).
!demo/clips/sidewalk-640.mp4

# 모델은 저장소에 안 넣는다 (허깅페이스에서 받는다). 테스트는 여기 있으면 돈다.
model.onnx
*.tgz

# 파이썬 스크립트는 이 저장소에 안 넣는다
*.py
