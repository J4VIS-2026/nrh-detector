# 변경 이력

**파일로 전달하는 동안에는 이 파일이 유일한 변경 통지다.** 새 `.tgz`를 받으면 여기부터
보면 된다.

출력이 달라지는 수정은 버그 수정이라도 minor 를 올린다. **patch 만 다르면 답이 같다는 뜻이다.**

---

## 0.10.1 — 2026-08-24

**출력은 그대로다.** 공개 저장소로 내기 전에 정리한 것이라 쓰는 쪽 코드도 결과도
바뀌지 않는다.

### 정리한 것

**만든 사람 PC의 경로를 걷어냈다.** 검증셋 이미지·촬영 영상·샘플 폴더를 코드에 적어
뒀는데 환경변수로만 받게 했다 — `NRH_VAL_IMAGES`, `NRH_VIDEOS`, `NRH_VIDEOS_SMALL`,
`NRH_SAMPLES`, `NRH_VAL_LIST`. 없으면 그 항목만 안 뜨고 나머지는 그대로 돈다.

**모델을 찾는 자리가 바뀌었다.** 다른 폴더를 보던 것을 **저장소
최상위의 `model.onnx`**로 옮겼다. 테스트와 데모 서버가 같은 자리를 본다.

**모델 받는 곳을 README 에 적었다.** 허깅페이스에 올려 두었다.

### 고친 것

- `native/gmc.ts` 의 주석이 "RANSAC 이 없다"고 했는데 아홉 줄 아래에서 부르고 있었다
- `core/gmc.ts` 에서 함수 설명이 상수 위에 붙어 IDE 툴팁에 엉뚱하게 떴다
- 모델 크기 표기 3.3MB → 3.5MB

### 문서

주석에서 코드를 되풀이한 것을 덜어내고, 같은 사실이 여러 곳에 있던 것을 한 곳으로
모았다. 측정 기록과 대조 근거는 그대로 두었다.

## 0.10.0 — 2026-08-16

**추적 기본값이 바뀌었다.** 쓰는 쪽 코드는 안 고쳐도 되지만 **결과가 달라진다.**

### 바뀐 것

**`match_thresh` 기본값 0.8 → 0.9**

값이 클수록 관대하게 잇는다. 직접 찍은 영상에 **사람이 트랙 하나하나에 정답을 달아**
견줬다 — 자동 지표만으로는 "트랙이 줄었다"가 좋은 일인지 나쁜 일인지 안 갈린다.

| 잣대 | 0.8 | **0.9** |
|---|---|---|
| 쪼개짐 (트랙 ÷ 실제 물체) | 1.383배 | **1.217배** |
| ≥5프레임 연속성 | 83.0% | **85.6%** |
| 혼입 (한 트랙에 여러 물체) | 3건 | **2건** |

**튜닝에 쓰지 않은 영상 2편에서도 확인했다** — 쪼개짐 1.240 → 1.120배, 연속성
67.4 → 73.7%. 개발 구간에 맞춘 값이 아니다.

**AI Hub 출처 표기가 들어갔다.** 이용 조건상 필수인데 0.9.0에 빠져 있었다. `NOTICE.md`
참고.

**전처리가 빨라졌다.** 태블릿 전처리 41 → 31ms. 해상도가 같으면 버퍼를 다시 쓴다.

### 패키지 쪽에서 고친 것

- **`_app/`을 `git archive`로 뜬다.** 폴더를 통째로 복사하던 때는 `.gitignore` 된 것이
  딸려 갔다 — 검증셋 사진이 든 백업 APK(333MB)와 만든 사람 PC의 `local.properties`가
  들어가 있었다. **둘 다 뺐다.**
- **`@babel/preset-typescript`를 7.x로 내렸다.** 이것만 8 계열이라 `npm install`도
  `npm ci`도 ERESOLVE로 깨졌다. 이제 클린 설치가 된다.
- `시작하기.md`에 **`JAVA_HOME` 설정**을 적었다. 안 잡혀 있으면 APK 빌드가 실패한다.

### 0.9.0 그대로 쓰려면

```js
new Tracker({ match_thresh: 0.8 })
```

**반대로 0.9.0에서 새 값만 쓰고 싶으면** `new Tracker({ match_thresh: 0.9 })`. 다만
출처 표기와 전처리 개선은 인자로 못 가져간다.

### 안 바꾼 것 — 근거를 남긴다

**`new_track_thresh` 0.35**는 쪼개짐이 1.117배로 좋아 보였으나 **탐지 60개를 놓치고
추가로 얻는 것은 0건**이었다. 쪼개진 트랙은 짧은 조각을 버리면 되지만 **아예 안 생긴
트랙은 되살릴 수 없다.**

**ByteTrack 2단계 연관은 동작하지 않는다.** 탐지기가 conf 0.25에서 먼저 잘라 저신뢰
탐지(전체의 24.2%)가 트래커에 안 온다. 살려봤으나 트랙 1개 차이라 구조를 안 바꿨다 —
`match_thresh` 0.9가 이미 관대해 1차에서 대부분 붙기 때문이다.

---

## 0.9.0 — 2026-08-07

**첫 배포.** 웹과 React Native 에서 도는 탐지·추적 모듈이다.

### 무엇이 되나

- **탐지** — `Detector.open()` / `det.detect()`. 웹은 canvas, RN은 opencv로 줄인다
- **추적** — `new Tracker()` / `T.update()`. BoT-SORT를 numpy 없이 직접 짰다
- **흔들림 보정** — `createGmc()`. 웹은 `sparseOptFlow`를 직접 짰고, RN은 ORB를 쓴다
- **클래스 맵** — `.onnx` 파일에서 직접 읽는다. ORT의 JS API가 안 주기 때문이다
- **입력 크기** — 모델에서 읽는다. 640을 코드에 고정하지 않아 480 재학습 모델도 돈다

### 기준값과 같은 답을 내나

| | mAP50 | mAP50-95 |
|---|---|---|
| 기준값 (opencv `INTER_AREA`) | 0.6828 | 0.4525 |
| **브라우저 (canvas)** | **0.6826** | 0.4518 |

검증셋 2000장, conf 0.001. **채점은 기준값을 낼 때와 같은 채점기가 했다** — 채점기를 따로
짜면 숫자가 달라져도 구현 차이인지 채점 차이인지 못 가린다.

같은 텐서를 넣으면 **좌표까지 정확히 같다**(`test/endtoend.test.js`). 기기에서도 탐지
9개가 기준값과 좌표까지 일치했다.

### 속도

| | 프레임당 | 초당 |
|---|---|---|
| 브라우저 (Chrome, Ryzen 7 7800X3D) | 168 ms | 6.0장 |
| Galaxy Z Flip 5 (식은 상태) | 74~110 ms | 9~13장 |
| Galaxy Tab S6 Lite | 341~384 ms | 2.6~2.9장 |

**발열이 2배를 가른다.** 7분을 연속으로 돌리며 쟀다.

| 경과 | 초당 |
|---|---|
| 0~30초 | **12장** |
| 30~60초 | 8장 |
| **2분 이후** | **5.7장에서 평평** |

**2분이면 절반이 되고 거기서 멈춘다.** 계속 나빠지지는 않지만 **처음 30초 값으로
계획을 세우면 안 된다.** 추론이 47ms에서 96ms로 같이 움직인다 - 계산량은 그대로이므로
클럭이 내려간 것이다.

> ⚠️ **선풍기를 틀어놓고 책상에서 잰 값이다.** 밖에서 손에 들고 걸었을 때는 **3~4장**
> 이었다. 맨 조건의 평형은 아직 안 쟀다.

### 흔들림 보정을 31ms에서 14ms로 줄였다

플립5에서 「추적」이 30ms로 프레임의 4분의 1을 먹고 있었다. 단계별로 재보니 **RANSAC이
15.4ms - 보정 전체의 절반**이었다.

- **RANSAC을 일찍 끊는다.** 정상값이 많으면 다섯 번이면 되는데 200번을 돌고 있었다.
  다만 최소 20회는 깔았다 - 안 깔면 기준값과의 차이가 15.6%에서 28.1%로 벌어진다
- **지난 프레임의 ORB를 다시 안 구한다.** 매 프레임 두 번 구하고 있었다

**`Float64Array`로 편 것이 컸다.** 같은 코드가 데스크톱 V8에서 0.103ms인데 폰에서
15.4ms였다 - Hermes가 배열 구조분해를 안 없애준다. **웹에서는 원래 문제가 아니었다.**

### 알아둘 것

- **의존성 0개.** `onnxruntime-web` 또는 `onnxruntime-react-native`만 `peerDependencies`다
- **모델은 패키지에 없다.** 따로 넣어야 한다
- **`webgpu`를 쓰지 마라.** 예외 없이 탐지 7,999개를 낸다
- **`COOP`/`COEP`를 붙여도 안 빨라진다.** 1스레드 179ms, 4스레드 180ms
- **`nnapi`·`xnnpack`을 켜지 마라.** 2.4~2.8배 느리다

### 아직 없는 것

- 영상 파일 처리 (`detect_video`에 해당하는 것)
- 그리기 도우미 — `demo/live.html`이 canvas로 그리는 예다
